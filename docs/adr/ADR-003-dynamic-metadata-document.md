# ADR-003 — Dynamic Metadata Document

Status: Accepted

AI metadata is stored as dynamic PostgreSQL JSONB. Optional profiles may validate documents, but arbitrary AI fields do not become fixed database columns.
