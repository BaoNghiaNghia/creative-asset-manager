# ADR-004 — Stable Search Projection

Status: Accepted

Elasticsearch indexes an explicit, versioned projection rather than arbitrary AI metadata keys. The projection is rebuildable from PostgreSQL without another AI request.
