import { useCallback, useEffect, useState } from "react";
import { inventoryLifecycleApi, type InventoryLifecycleHistoryItem, type InventoryLifecycleStage, type InventoryLifecycleStageStatus } from "./api";
const labels:Record<string,string>={morning_reset:"Reset đầu ngày",daily_check:"Đang kiểm kho",afternoon_snapshot:"Đang snapshot",evening_reconcile:"Đang đối soát",verified:"Cần xác minh",completed:"Hoàn tất"};
const symbols:Record<InventoryLifecycleStageStatus,string>={pending:"○",scheduled:"◌",running:"●",completed:"✓",blocked:"!",review_required:"!",failed:"×",stale:"×"};
function format(value:string|null){return value?new Intl.DateTimeFormat("vi-VN",{dateStyle:"short",timeStyle:"short"}).format(new Date(value)):"—"}
function Stage({stage,onClick}:{stage:InventoryLifecycleStage;onClick:()=>void}){return <button type="button" className={`inventory-pipeline-stage ${stage.status}`} onClick={onClick} title={`${stage.label}: ${stage.status}`}><span aria-hidden="true">{symbols[stage.status]}</span><small>{stage.label.replace(" đầu ngày","").replace("Kiểm kho trong ngày","Kiểm kho")}</small></button>}
function Details({item,onClose}:{item:InventoryLifecycleHistoryItem;onClose:()=>void}){return <aside className="inventory-pipeline-details" role="dialog" aria-label="Chi tiết tiến trình"><button onClick={onClose} aria-label="Đóng">×</button><h3>{item.business_date}</h3><p>Giai đoạn hiện tại: <b>{labels[item.current_stage]||item.current_stage}</b></p>{item.stages.map(s=><dl key={s.key}><dt>{s.label} · {s.status}</dt><dd>Lịch: {s.scheduled_time||"—"}<br/>Bắt đầu: {format(s.started_at||null)}<br/>Hoàn tất: {format(s.completed_at||null)}{s.error_code?<><br/>Lỗi: {s.error_code}</>:null}{s.run_id?<><br/>Run: {s.run_id}</>:null}{s.plan_hash?<><br/>Plan: {s.plan_hash}</>:null}</dd></dl>)}</aside>}
export function InventoryDailyPipeline(){const [data,setData]=useState<{items:InventoryLifecycleHistoryItem[];page:number;page_size:number;pages:number}|null>(null);const [page,setPage]=useState(1);const [pageSize,setPageSize]=useState(25);const [loading,setLoading]=useState(true);const [error,setError]=useState(false);const [selected,setSelected]=useState<InventoryLifecycleHistoryItem|null>(null);const load=useCallback(async()=>{setLoading(true);setError(false);try{setData(await inventoryLifecycleApi.getHistory(page,pageSize))}catch{setError(true)}finally{setLoading(false)}},[page,pageSize]);useEffect(()=>{void load()},[load]);useEffect(()=>{let timer:number|undefined;const arm=()=>{if(timer)window.clearInterval(timer);timer=undefined;if(document.visibilityState==="visible")timer=window.setInterval(()=>void load(),30000)};arm();document.addEventListener("visibilitychange",arm);return()=>{if(timer)window.clearInterval(timer);document.removeEventListener("visibilitychange",arm)}},[load]);if(loading&&!data)return <section className="inventory-empty">Đang tải tiến trình Inventory…</section>;if(error&&!data)return <section className="inventory-empty"><p>Không thể tải tiến trình.</p><button onClick={()=>void load()}>Thử lại</button></section>;return <section className="inventory-pipeline"><header><div><span>INVENTORY</span><h2>Tiến trình kiểm kho</h2><p>Theo dõi mỗi ngày làm việc từ reset đến xác minh Gemini.</p></div><button onClick={()=>void load()} disabled={loading}>{loading?"Đang làm mới…":"Làm mới"}</button></header>{error?<p className="inventory-error">Không thể tải dữ liệu mới nhất.</p>:null}{!data?.items.length?<div className="inventory-pipeline-empty"><b>Chưa có tiến trình kiểm kho.</b><span>Tiến trình của ngày làm việc sẽ xuất hiện ở đây khi automation bắt đầu.</span></div>:<div className="inventory-pipeline-scroll"><table><thead><tr><th>NGÀY KIỂM</th><th>FILE</th><th>GIAI ĐOẠN HIỆN TẠI</th><th>LUỒNG XỬ LÝ</th><th>CẬP NHẬT</th><th>CẦN XỬ LÝ</th></tr></thead><tbody>{data.items.map(item=><tr key={item.business_date} onClick={()=>setSelected(item)}><td><b>{new Intl.DateTimeFormat("vi-VN",{dateStyle:"medium"}).format(new Date(`${item.business_date}T00:00:00`))}</b></td><td className="inventory-pipeline-files">{item.files.shared_url?<a href={item.files.shared_url} target="_blank" rel="noreferrer">Shared ↗</a>:null}{item.files.snapshot_url?<a href={item.files.snapshot_url} target="_blank" rel="noreferrer">Snapshot ↗</a>:<span>Snapshot: Chưa tạo</span>}{item.files.gemini_url?<a href={item.files.gemini_url} target="_blank" rel="noreferrer">Gemini ↗</a>:null}</td><td><span className={`inventory-pipeline-chip ${item.overall_status}`}>{labels[item.current_stage]||item.current_stage}</span></td><td><div className="inventory-pipeline-flow">{item.stages.map((stage,index)=><div key={stage.key} className="inventory-pipeline-node"><Stage stage={stage} onClick={()=>setSelected(item)}/>{index<item.stages.length-1?<i className={stage.status==="completed"?"done":""}/>:null}</div>)}</div></td><td>{format(item.updated_at)}</td><td>{item.action_required?<button className="inventory-pipeline-action" onClick={e=>{e.stopPropagation();setSelected(item)}}>{item.action_required.label}</button>:"—"}</td></tr>)}</tbody></table></div>}<footer><label>Số mục mỗi trang <select value={pageSize} onChange={e=>{setPageSize(Number(e.target.value));setPage(1)}}>{[25,50,100].map(n=><option key={n}>{n}</option>)}</select></label><div><button disabled={!data||page===1} onClick={()=>setPage(page-1)}>Trước</button><b>{data?.page||1} / {data?.pages||1}</b><button disabled={!data||page>=(data?.pages||1)} onClick={()=>setPage(page+1)}>Tiếp</button></div></footer>{selected?<Details item={selected} onClose={()=>setSelected(null)}/>:null}</section>}
