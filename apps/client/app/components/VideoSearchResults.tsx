import { useState } from "react";
import type { VideoSearchItem } from "../hooks/useVideoSearch";
export function formatVideoTimestamp(milliseconds: number): string { const seconds = Math.max(0, Math.floor(milliseconds / 1000)); const hours = Math.floor(seconds / 3600); const minutes = Math.floor((seconds % 3600) / 60); const remainder = String(seconds % 60).padStart(2, "0"); return hours ? hours + ":" + String(minutes).padStart(2, "0") + ":" + remainder : String(minutes).padStart(2, "0") + ":" + remainder; }
export function formatVideoDuration(milliseconds: number | null): string | null { return typeof milliseconds === "number" && milliseconds >= 0 ? formatVideoTimestamp(milliseconds) : null; }
function VideoThumbnail({ item }: { item: VideoSearchItem }) { const [failed, setFailed] = useState(false); if (!item.thumbnail_url || failed) return <div className="video-search-thumbnail video-search-thumbnail-fallback" role="img" aria-label={"Video placeholder for " + item.filename}><span aria-hidden="true">Play</span></div>; return <div className="video-search-thumbnail"><img src={item.thumbnail_url} alt={"Thumbnail for " + item.filename} loading="lazy" referrerPolicy="no-referrer" onError={() => setFailed(true)} /><span aria-hidden="true">Play</span></div>; }
export function VideoSearchResults({ items, onOpen }: { items: VideoSearchItem[]; onOpen: (item: VideoSearchItem) => void }) { return <div className="video-search-grid" aria-label="Video search results">{items.map(item => { const excerpt = item.best_match.visual_description || item.best_match.speech; const duration = formatVideoDuration(item.duration_ms); return <article className="video-search-card" key={item.analysis_run_id} aria-label={"Video result: " + item.filename}><VideoThumbnail item={item} /><div className="video-search-card-body"><h2 title={item.filename}>{item.filename}</h2><div className="video-search-meta">{duration && <span>Duration {duration}</span>}<span>{item.matches.length} matching segment{item.matches.length === 1 ? "" : "s"}</span></div><div className="video-search-best-match"><b>Best match - {formatVideoTimestamp(item.best_match.start_ms)}</b><p>{item.best_match.summary}</p>{excerpt && <small>{excerpt}</small>}</div><button type="button" className="video-search-open" onClick={() => onOpen(item)} aria-label={"Open " + item.filename + " at " + formatVideoTimestamp(item.best_match.start_ms)}>Open at {formatVideoTimestamp(item.best_match.start_ms)}</button></div></article>; })}</div>; }
