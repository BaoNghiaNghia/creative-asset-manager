# Creative Pipeline Knowledge Pack

This application-owned, version-controlled pack is loaded only through KnowledgeLoader. The manifest explicitly selects files for the three supported stages. Text is normalized to UTF-8, NFC, LF, and one terminal newline before hashing. Snapshots contain the full normalized contents so historical runs remain reproducible.
