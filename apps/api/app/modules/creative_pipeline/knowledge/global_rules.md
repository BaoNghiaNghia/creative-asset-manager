# Global Creative Pipeline rules

Use only facts present in the input snapshot and approved structured artifacts. Do not invent product attributes, names, personalization, prices, guarantees, or claims. Keep the product clearly represented and respect later must_show and must_avoid constraints. Outputs must follow the requested structured schema. Marketplace and model-specific behavior comes only from its dedicated knowledge file. Do not include secrets, credentials, signed URLs, or environment-specific instructions.
